// Generated by Ignite ignite.com/cli
import { SigningStargateClient } from "@cosmjs/stargate";
import { Registry } from "@cosmjs/proto-signing";
import { msgTypes } from './registry';
import { Api } from "./rest";
import { MsgUpdateParams } from "./types/ferac/ferac/v1/tx";
import { MsgAnnounceValidatorExit } from "./types/ferac/ferac/v1/tx";
import { MsgSetValidatorQuality } from "./types/ferac/ferac/v1/tx";
import { Params as typeParams } from "./types";
import { RestrictedAccount as typeRestrictedAccount } from "./types";
import { ValidatorReserve as typeValidatorReserve } from "./types";
import { ValidatorPerformance as typeValidatorPerformance } from "./types";
import { ValidatorExit as typeValidatorExit } from "./types";
export { MsgUpdateParams, MsgAnnounceValidatorExit, MsgSetValidatorQuality };
export const registry = new Registry(msgTypes);
function getStructure(template) {
    const structure = { fields: [] };
    for (let [key, value] of Object.entries(template)) {
        let field = { name: key, type: typeof value };
        structure.fields.push(field);
    }
    return structure;
}
const defaultFee = {
    amount: [],
    gas: "200000",
};
export const txClient = ({ signer, prefix, addr } = { addr: "http://localhost:26657", prefix: "cosmos" }) => {
    return {
        async sendMsgUpdateParams({ value, fee, memo }) {
            if (!signer) {
                throw new Error('TxClient:sendMsgUpdateParams: Unable to sign Tx. Signer is not present.');
            }
            try {
                const { address } = (await signer.getAccounts())[0];
                const signingClient = await SigningStargateClient.connectWithSigner(addr, signer, { registry });
                let msg = this.msgUpdateParams({ value: MsgUpdateParams.fromPartial(value) });
                return await signingClient.signAndBroadcast(address, [msg], fee ? fee : defaultFee, memo);
            }
            catch (e) {
                throw new Error('TxClient:sendMsgUpdateParams: Could not broadcast Tx: ' + e.message);
            }
        },
        async sendMsgAnnounceValidatorExit({ value, fee, memo }) {
            if (!signer) {
                throw new Error('TxClient:sendMsgAnnounceValidatorExit: Unable to sign Tx. Signer is not present.');
            }
            try {
                const { address } = (await signer.getAccounts())[0];
                const signingClient = await SigningStargateClient.connectWithSigner(addr, signer, { registry });
                let msg = this.msgAnnounceValidatorExit({ value: MsgAnnounceValidatorExit.fromPartial(value) });
                return await signingClient.signAndBroadcast(address, [msg], fee ? fee : defaultFee, memo);
            }
            catch (e) {
                throw new Error('TxClient:sendMsgAnnounceValidatorExit: Could not broadcast Tx: ' + e.message);
            }
        },
        async sendMsgSetValidatorQuality({ value, fee, memo }) {
            if (!signer) {
                throw new Error('TxClient:sendMsgSetValidatorQuality: Unable to sign Tx. Signer is not present.');
            }
            try {
                const { address } = (await signer.getAccounts())[0];
                const signingClient = await SigningStargateClient.connectWithSigner(addr, signer, { registry });
                let msg = this.msgSetValidatorQuality({ value: MsgSetValidatorQuality.fromPartial(value) });
                return await signingClient.signAndBroadcast(address, [msg], fee ? fee : defaultFee, memo);
            }
            catch (e) {
                throw new Error('TxClient:sendMsgSetValidatorQuality: Could not broadcast Tx: ' + e.message);
            }
        },
        msgUpdateParams({ value }) {
            try {
                return { typeUrl: "/ferac.ferac.v1.MsgUpdateParams", value: MsgUpdateParams.fromPartial(value) };
            }
            catch (e) {
                throw new Error('TxClient:MsgUpdateParams: Could not create message: ' + e.message);
            }
        },
        msgAnnounceValidatorExit({ value }) {
            try {
                return { typeUrl: "/ferac.ferac.v1.MsgAnnounceValidatorExit", value: MsgAnnounceValidatorExit.fromPartial(value) };
            }
            catch (e) {
                throw new Error('TxClient:MsgAnnounceValidatorExit: Could not create message: ' + e.message);
            }
        },
        msgSetValidatorQuality({ value }) {
            try {
                return { typeUrl: "/ferac.ferac.v1.MsgSetValidatorQuality", value: MsgSetValidatorQuality.fromPartial(value) };
            }
            catch (e) {
                throw new Error('TxClient:MsgSetValidatorQuality: Could not create message: ' + e.message);
            }
        },
    };
};
export const queryClient = ({ addr: addr } = { addr: "http://localhost:1317" }) => {
    return new Api({ baseURL: addr });
};
class SDKModule {
    constructor(client) {
        this.registry = [];
        this.query = queryClient({ addr: client.env.apiURL });
        this.updateTX(client);
        this.structure = {
            Params: getStructure(typeParams.fromPartial({})),
            RestrictedAccount: getStructure(typeRestrictedAccount.fromPartial({})),
            ValidatorReserve: getStructure(typeValidatorReserve.fromPartial({})),
            ValidatorPerformance: getStructure(typeValidatorPerformance.fromPartial({})),
            ValidatorExit: getStructure(typeValidatorExit.fromPartial({})),
        };
        client.on('signer-changed', (signer) => {
            this.updateTX(client);
        });
    }
    updateTX(client) {
        const methods = txClient({
            signer: client.signer,
            addr: client.env.rpcURL,
            prefix: client.env.prefix ?? "cosmos",
        });
        this.tx = methods;
        for (let m in methods) {
            this.tx[m] = methods[m].bind(this.tx);
        }
    }
}
;
const IgntModule = (test) => {
    return {
        module: {
            FeracFeracV_1: new SDKModule(test)
        },
        registry: msgTypes
    };
};
export default IgntModule;
