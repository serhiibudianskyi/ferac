// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: ferac/ferac/v1/genesis.proto
/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Params } from "./params";
import { RestrictedAccount, ValidatorPerformance, ValidatorReserve } from "./tokenomics";
export const protobufPackage = "ferac.ferac.v1";
function createBaseGenesisState() {
    return {
        params: undefined,
        restrictedAccounts: [],
        validatorReserve: undefined,
        performances: [],
        reserveFunder: "",
    };
}
export const GenesisState = {
    encode(message, writer = new BinaryWriter()) {
        if (message.params !== undefined) {
            Params.encode(message.params, writer.uint32(10).fork()).join();
        }
        for (const v of message.restrictedAccounts) {
            RestrictedAccount.encode(v, writer.uint32(18).fork()).join();
        }
        if (message.validatorReserve !== undefined) {
            ValidatorReserve.encode(message.validatorReserve, writer.uint32(26).fork()).join();
        }
        for (const v of message.performances) {
            ValidatorPerformance.encode(v, writer.uint32(34).fork()).join();
        }
        if (message.reserveFunder !== "") {
            writer.uint32(42).string(message.reserveFunder);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = reader.__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error("protobuf decode recursion limit exceeded");
        }
        reader.__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseGenesisState();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }
                        message.params = Params.decode(reader, reader.uint32());
                        continue;
                    }
                    case 2: {
                        if (tag !== 18) {
                            break;
                        }
                        message.restrictedAccounts.push(RestrictedAccount.decode(reader, reader.uint32()));
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }
                        message.validatorReserve = ValidatorReserve.decode(reader, reader.uint32());
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }
                        message.performances.push(ValidatorPerformance.decode(reader, reader.uint32()));
                        continue;
                    }
                    case 5: {
                        if (tag !== 42) {
                            break;
                        }
                        message.reserveFunder = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        }
        finally {
            reader.__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },
    fromJSON(object) {
        return {
            params: isSet(object.params) ? Params.fromJSON(object.params) : undefined,
            restrictedAccounts: globalThis.Array.isArray(object?.restrictedAccounts)
                ? object.restrictedAccounts.map((e) => RestrictedAccount.fromJSON(e))
                : globalThis.Array.isArray(object?.restricted_accounts)
                    ? object.restricted_accounts.map((e) => RestrictedAccount.fromJSON(e))
                    : [],
            validatorReserve: isSet(object.validatorReserve)
                ? ValidatorReserve.fromJSON(object.validatorReserve)
                : isSet(object.validator_reserve)
                    ? ValidatorReserve.fromJSON(object.validator_reserve)
                    : undefined,
            performances: globalThis.Array.isArray(object?.performances)
                ? object.performances.map((e) => ValidatorPerformance.fromJSON(e))
                : [],
            reserveFunder: isSet(object.reserveFunder)
                ? globalThis.String(object.reserveFunder)
                : isSet(object.reserve_funder)
                    ? globalThis.String(object.reserve_funder)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.params !== undefined) {
            obj.params = Params.toJSON(message.params);
        }
        if (message.restrictedAccounts?.length) {
            obj.restrictedAccounts = message.restrictedAccounts.map((e) => RestrictedAccount.toJSON(e));
        }
        if (message.validatorReserve !== undefined) {
            obj.validatorReserve = ValidatorReserve.toJSON(message.validatorReserve);
        }
        if (message.performances?.length) {
            obj.performances = message.performances.map((e) => ValidatorPerformance.toJSON(e));
        }
        if (message.reserveFunder !== "") {
            obj.reserveFunder = message.reserveFunder;
        }
        return obj;
    },
    create(base) {
        return GenesisState.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseGenesisState();
        message.params = (object.params !== undefined && object.params !== null)
            ? Params.fromPartial(object.params)
            : undefined;
        message.restrictedAccounts = object.restrictedAccounts?.map((e) => RestrictedAccount.fromPartial(e)) || [];
        message.validatorReserve = (object.validatorReserve !== undefined && object.validatorReserve !== null)
            ? ValidatorReserve.fromPartial(object.validatorReserve)
            : undefined;
        message.performances = object.performances?.map((e) => ValidatorPerformance.fromPartial(e)) || [];
        message.reserveFunder = object.reserveFunder ?? "";
        return message;
    },
};
function isSet(value) {
    return value !== null && value !== undefined;
}
