// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: ferac/ferac/v1/tokenomics.proto
/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
export const protobufPackage = "ferac.ferac.v1";
/** AllocationCategory enumerates the genesis distribution buckets. */
export var AllocationCategory;
(function (AllocationCategory) {
    AllocationCategory[AllocationCategory["ALLOCATION_CATEGORY_UNSPECIFIED"] = 0] = "ALLOCATION_CATEGORY_UNSPECIFIED";
    AllocationCategory[AllocationCategory["ALLOCATION_CATEGORY_CREATOR"] = 1] = "ALLOCATION_CATEGORY_CREATOR";
    AllocationCategory[AllocationCategory["ALLOCATION_CATEGORY_TEAM"] = 2] = "ALLOCATION_CATEGORY_TEAM";
    AllocationCategory[AllocationCategory["ALLOCATION_CATEGORY_VALIDATOR_RESERVE"] = 3] = "ALLOCATION_CATEGORY_VALIDATOR_RESERVE";
    AllocationCategory[AllocationCategory["ALLOCATION_CATEGORY_COMMUNITY"] = 4] = "ALLOCATION_CATEGORY_COMMUNITY";
    AllocationCategory[AllocationCategory["ALLOCATION_CATEGORY_DEX_LIQUIDITY"] = 5] = "ALLOCATION_CATEGORY_DEX_LIQUIDITY";
    AllocationCategory[AllocationCategory["ALLOCATION_CATEGORY_TREASURY"] = 6] = "ALLOCATION_CATEGORY_TREASURY";
    AllocationCategory[AllocationCategory["ALLOCATION_CATEGORY_ECOSYSTEM"] = 7] = "ALLOCATION_CATEGORY_ECOSYSTEM";
    AllocationCategory[AllocationCategory["UNRECOGNIZED"] = -1] = "UNRECOGNIZED";
})(AllocationCategory || (AllocationCategory = {}));
export function allocationCategoryFromJSON(object) {
    switch (object) {
        case 0:
        case "ALLOCATION_CATEGORY_UNSPECIFIED":
            return AllocationCategory.ALLOCATION_CATEGORY_UNSPECIFIED;
        case 1:
        case "ALLOCATION_CATEGORY_CREATOR":
            return AllocationCategory.ALLOCATION_CATEGORY_CREATOR;
        case 2:
        case "ALLOCATION_CATEGORY_TEAM":
            return AllocationCategory.ALLOCATION_CATEGORY_TEAM;
        case 3:
        case "ALLOCATION_CATEGORY_VALIDATOR_RESERVE":
            return AllocationCategory.ALLOCATION_CATEGORY_VALIDATOR_RESERVE;
        case 4:
        case "ALLOCATION_CATEGORY_COMMUNITY":
            return AllocationCategory.ALLOCATION_CATEGORY_COMMUNITY;
        case 5:
        case "ALLOCATION_CATEGORY_DEX_LIQUIDITY":
            return AllocationCategory.ALLOCATION_CATEGORY_DEX_LIQUIDITY;
        case 6:
        case "ALLOCATION_CATEGORY_TREASURY":
            return AllocationCategory.ALLOCATION_CATEGORY_TREASURY;
        case 7:
        case "ALLOCATION_CATEGORY_ECOSYSTEM":
            return AllocationCategory.ALLOCATION_CATEGORY_ECOSYSTEM;
        case -1:
        case "UNRECOGNIZED":
        default:
            return AllocationCategory.UNRECOGNIZED;
    }
}
export function allocationCategoryToJSON(object) {
    switch (object) {
        case AllocationCategory.ALLOCATION_CATEGORY_UNSPECIFIED:
            return "ALLOCATION_CATEGORY_UNSPECIFIED";
        case AllocationCategory.ALLOCATION_CATEGORY_CREATOR:
            return "ALLOCATION_CATEGORY_CREATOR";
        case AllocationCategory.ALLOCATION_CATEGORY_TEAM:
            return "ALLOCATION_CATEGORY_TEAM";
        case AllocationCategory.ALLOCATION_CATEGORY_VALIDATOR_RESERVE:
            return "ALLOCATION_CATEGORY_VALIDATOR_RESERVE";
        case AllocationCategory.ALLOCATION_CATEGORY_COMMUNITY:
            return "ALLOCATION_CATEGORY_COMMUNITY";
        case AllocationCategory.ALLOCATION_CATEGORY_DEX_LIQUIDITY:
            return "ALLOCATION_CATEGORY_DEX_LIQUIDITY";
        case AllocationCategory.ALLOCATION_CATEGORY_TREASURY:
            return "ALLOCATION_CATEGORY_TREASURY";
        case AllocationCategory.ALLOCATION_CATEGORY_ECOSYSTEM:
            return "ALLOCATION_CATEGORY_ECOSYSTEM";
        case AllocationCategory.UNRECOGNIZED:
        default:
            return "UNRECOGNIZED";
    }
}
function createBaseRestrictedAccount() {
    return { address: "", category: 0, remaining: "", periodLimit: "", released: "", periodStart: 0 };
}
export const RestrictedAccount = {
    encode(message, writer = new BinaryWriter()) {
        if (message.address !== "") {
            writer.uint32(10).string(message.address);
        }
        if (message.category !== 0) {
            writer.uint32(16).int32(message.category);
        }
        if (message.remaining !== "") {
            writer.uint32(26).string(message.remaining);
        }
        if (message.periodLimit !== "") {
            writer.uint32(34).string(message.periodLimit);
        }
        if (message.released !== "") {
            writer.uint32(42).string(message.released);
        }
        if (message.periodStart !== 0) {
            writer.uint32(48).int64(message.periodStart);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = reader.__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error("protobuf decode recursion limit exceeded");
        }
        reader.__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseRestrictedAccount();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }
                        message.address = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 16) {
                            break;
                        }
                        message.category = reader.int32();
                        continue;
                    }
                    case 3: {
                        if (tag !== 26) {
                            break;
                        }
                        message.remaining = reader.string();
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }
                        message.periodLimit = reader.string();
                        continue;
                    }
                    case 5: {
                        if (tag !== 42) {
                            break;
                        }
                        message.released = reader.string();
                        continue;
                    }
                    case 6: {
                        if (tag !== 48) {
                            break;
                        }
                        message.periodStart = longToNumber(reader.int64());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        }
        finally {
            reader.__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },
    fromJSON(object) {
        return {
            address: isSet(object.address) ? globalThis.String(object.address) : "",
            category: isSet(object.category) ? allocationCategoryFromJSON(object.category) : 0,
            remaining: isSet(object.remaining) ? globalThis.String(object.remaining) : "",
            periodLimit: isSet(object.periodLimit)
                ? globalThis.String(object.periodLimit)
                : isSet(object.period_limit)
                    ? globalThis.String(object.period_limit)
                    : "",
            released: isSet(object.released) ? globalThis.String(object.released) : "",
            periodStart: isSet(object.periodStart)
                ? globalThis.Number(object.periodStart)
                : isSet(object.period_start)
                    ? globalThis.Number(object.period_start)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.address !== "") {
            obj.address = message.address;
        }
        if (message.category !== 0) {
            obj.category = allocationCategoryToJSON(message.category);
        }
        if (message.remaining !== "") {
            obj.remaining = message.remaining;
        }
        if (message.periodLimit !== "") {
            obj.periodLimit = message.periodLimit;
        }
        if (message.released !== "") {
            obj.released = message.released;
        }
        if (message.periodStart !== 0) {
            obj.periodStart = Math.round(message.periodStart);
        }
        return obj;
    },
    create(base) {
        return RestrictedAccount.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseRestrictedAccount();
        message.address = object.address ?? "";
        message.category = object.category ?? 0;
        message.remaining = object.remaining ?? "";
        message.periodLimit = object.periodLimit ?? "";
        message.released = object.released ?? "";
        message.periodStart = object.periodStart ?? 0;
        return message;
    },
};
function createBaseValidatorReserve() {
    return { remaining: "", lastEpochTime: 0, epoch: 0 };
}
export const ValidatorReserve = {
    encode(message, writer = new BinaryWriter()) {
        if (message.remaining !== "") {
            writer.uint32(10).string(message.remaining);
        }
        if (message.lastEpochTime !== 0) {
            writer.uint32(16).int64(message.lastEpochTime);
        }
        if (message.epoch !== 0) {
            writer.uint32(24).uint64(message.epoch);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = reader.__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error("protobuf decode recursion limit exceeded");
        }
        reader.__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseValidatorReserve();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }
                        message.remaining = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 16) {
                            break;
                        }
                        message.lastEpochTime = longToNumber(reader.int64());
                        continue;
                    }
                    case 3: {
                        if (tag !== 24) {
                            break;
                        }
                        message.epoch = longToNumber(reader.uint64());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        }
        finally {
            reader.__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },
    fromJSON(object) {
        return {
            remaining: isSet(object.remaining) ? globalThis.String(object.remaining) : "",
            lastEpochTime: isSet(object.lastEpochTime)
                ? globalThis.Number(object.lastEpochTime)
                : isSet(object.last_epoch_time)
                    ? globalThis.Number(object.last_epoch_time)
                    : 0,
            epoch: isSet(object.epoch) ? globalThis.Number(object.epoch) : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.remaining !== "") {
            obj.remaining = message.remaining;
        }
        if (message.lastEpochTime !== 0) {
            obj.lastEpochTime = Math.round(message.lastEpochTime);
        }
        if (message.epoch !== 0) {
            obj.epoch = Math.round(message.epoch);
        }
        return obj;
    },
    create(base) {
        return ValidatorReserve.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseValidatorReserve();
        message.remaining = object.remaining ?? "";
        message.lastEpochTime = object.lastEpochTime ?? 0;
        message.epoch = object.epoch ?? 0;
        return message;
    },
};
function createBaseValidatorPerformance() {
    return { validatorAddress: "", signedBlocks: 0, totalBlocks: 0, qualityScore: "" };
}
export const ValidatorPerformance = {
    encode(message, writer = new BinaryWriter()) {
        if (message.validatorAddress !== "") {
            writer.uint32(10).string(message.validatorAddress);
        }
        if (message.signedBlocks !== 0) {
            writer.uint32(16).uint64(message.signedBlocks);
        }
        if (message.totalBlocks !== 0) {
            writer.uint32(24).uint64(message.totalBlocks);
        }
        if (message.qualityScore !== "") {
            writer.uint32(34).string(message.qualityScore);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = reader.__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error("protobuf decode recursion limit exceeded");
        }
        reader.__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseValidatorPerformance();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }
                        message.validatorAddress = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 16) {
                            break;
                        }
                        message.signedBlocks = longToNumber(reader.uint64());
                        continue;
                    }
                    case 3: {
                        if (tag !== 24) {
                            break;
                        }
                        message.totalBlocks = longToNumber(reader.uint64());
                        continue;
                    }
                    case 4: {
                        if (tag !== 34) {
                            break;
                        }
                        message.qualityScore = reader.string();
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        }
        finally {
            reader.__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },
    fromJSON(object) {
        return {
            validatorAddress: isSet(object.validatorAddress)
                ? globalThis.String(object.validatorAddress)
                : isSet(object.validator_address)
                    ? globalThis.String(object.validator_address)
                    : "",
            signedBlocks: isSet(object.signedBlocks)
                ? globalThis.Number(object.signedBlocks)
                : isSet(object.signed_blocks)
                    ? globalThis.Number(object.signed_blocks)
                    : 0,
            totalBlocks: isSet(object.totalBlocks)
                ? globalThis.Number(object.totalBlocks)
                : isSet(object.total_blocks)
                    ? globalThis.Number(object.total_blocks)
                    : 0,
            qualityScore: isSet(object.qualityScore)
                ? globalThis.String(object.qualityScore)
                : isSet(object.quality_score)
                    ? globalThis.String(object.quality_score)
                    : "",
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.validatorAddress !== "") {
            obj.validatorAddress = message.validatorAddress;
        }
        if (message.signedBlocks !== 0) {
            obj.signedBlocks = Math.round(message.signedBlocks);
        }
        if (message.totalBlocks !== 0) {
            obj.totalBlocks = Math.round(message.totalBlocks);
        }
        if (message.qualityScore !== "") {
            obj.qualityScore = message.qualityScore;
        }
        return obj;
    },
    create(base) {
        return ValidatorPerformance.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseValidatorPerformance();
        message.validatorAddress = object.validatorAddress ?? "";
        message.signedBlocks = object.signedBlocks ?? 0;
        message.totalBlocks = object.totalBlocks ?? 0;
        message.qualityScore = object.qualityScore ?? "";
        return message;
    },
};
function createBaseValidatorExit() {
    return { validatorAddress: "", announcedAt: 0, allowedAt: 0 };
}
export const ValidatorExit = {
    encode(message, writer = new BinaryWriter()) {
        if (message.validatorAddress !== "") {
            writer.uint32(10).string(message.validatorAddress);
        }
        if (message.announcedAt !== 0) {
            writer.uint32(16).int64(message.announcedAt);
        }
        if (message.allowedAt !== 0) {
            writer.uint32(24).int64(message.allowedAt);
        }
        return writer;
    },
    decode(input, length) {
        const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
        const previousRecursionDepth = reader.__tsProtoDecodeDepth ?? 0;
        if (previousRecursionDepth >= 100) {
            throw new globalThis.Error("protobuf decode recursion limit exceeded");
        }
        reader.__tsProtoDecodeDepth = previousRecursionDepth + 1;
        try {
            const end = length === undefined ? reader.len : reader.pos + length;
            const message = createBaseValidatorExit();
            while (reader.pos < end) {
                const tag = reader.uint32();
                switch (tag >>> 3) {
                    case 1: {
                        if (tag !== 10) {
                            break;
                        }
                        message.validatorAddress = reader.string();
                        continue;
                    }
                    case 2: {
                        if (tag !== 16) {
                            break;
                        }
                        message.announcedAt = longToNumber(reader.int64());
                        continue;
                    }
                    case 3: {
                        if (tag !== 24) {
                            break;
                        }
                        message.allowedAt = longToNumber(reader.int64());
                        continue;
                    }
                }
                if ((tag & 7) === 4 || tag === 0) {
                    break;
                }
                reader.skip(tag & 7);
            }
            return message;
        }
        finally {
            reader.__tsProtoDecodeDepth = previousRecursionDepth;
        }
    },
    fromJSON(object) {
        return {
            validatorAddress: isSet(object.validatorAddress)
                ? globalThis.String(object.validatorAddress)
                : isSet(object.validator_address)
                    ? globalThis.String(object.validator_address)
                    : "",
            announcedAt: isSet(object.announcedAt)
                ? globalThis.Number(object.announcedAt)
                : isSet(object.announced_at)
                    ? globalThis.Number(object.announced_at)
                    : 0,
            allowedAt: isSet(object.allowedAt)
                ? globalThis.Number(object.allowedAt)
                : isSet(object.allowed_at)
                    ? globalThis.Number(object.allowed_at)
                    : 0,
        };
    },
    toJSON(message) {
        const obj = {};
        if (message.validatorAddress !== "") {
            obj.validatorAddress = message.validatorAddress;
        }
        if (message.announcedAt !== 0) {
            obj.announcedAt = Math.round(message.announcedAt);
        }
        if (message.allowedAt !== 0) {
            obj.allowedAt = Math.round(message.allowedAt);
        }
        return obj;
    },
    create(base) {
        return ValidatorExit.fromPartial(base ?? {});
    },
    fromPartial(object) {
        const message = createBaseValidatorExit();
        message.validatorAddress = object.validatorAddress ?? "";
        message.announcedAt = object.announcedAt ?? 0;
        message.allowedAt = object.allowedAt ?? 0;
        return message;
    },
};
function longToNumber(int64) {
    const num = globalThis.Number(int64.toString());
    if (num > globalThis.Number.MAX_SAFE_INTEGER) {
        throw new globalThis.Error("Value is larger than Number.MAX_SAFE_INTEGER");
    }
    if (num < globalThis.Number.MIN_SAFE_INTEGER) {
        throw new globalThis.Error("Value is smaller than Number.MIN_SAFE_INTEGER");
    }
    return num;
}
function isSet(value) {
    return value !== null && value !== undefined;
}
