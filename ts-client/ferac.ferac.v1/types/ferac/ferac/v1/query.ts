// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.12.1
//   protoc               unknown
// source: ferac/ferac/v1/query.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { PageRequest, PageResponse } from "../../../cosmos/base/query/v1beta1/pagination";
import { Params } from "./params";
import { RestrictedAccount, ValidatorPerformance, ValidatorReserve } from "./tokenomics";

export const protobufPackage = "ferac.ferac.v1";

/** QueryParamsRequest is request type for the Query/Params RPC method. */
export interface QueryParamsRequest {
}

/** QueryParamsResponse is response type for the Query/Params RPC method. */
export interface QueryParamsResponse {
  /** params holds all the parameters of this module. */
  params: Params | undefined;
}

/** QueryValidatorReserveRequest is the request type for Query/ValidatorReserve. */
export interface QueryValidatorReserveRequest {
}

/** QueryValidatorReserveResponse is the response type for Query/ValidatorReserve. */
export interface QueryValidatorReserveResponse {
  reserve:
    | ValidatorReserve
    | undefined;
  /** next_release is the amount that will be released at the next epoch. */
  nextRelease: string;
}

/** QueryRestrictedAccountRequest is the request type for Query/RestrictedAccount. */
export interface QueryRestrictedAccountRequest {
  address: string;
}

/** QueryRestrictedAccountResponse is the response type for Query/RestrictedAccount. */
export interface QueryRestrictedAccountResponse {
  account:
    | RestrictedAccount
    | undefined;
  /** available is how much may still be transferred in the current period. */
  available: string;
}

/** QueryRestrictedAccountsRequest is the request type for Query/RestrictedAccounts. */
export interface QueryRestrictedAccountsRequest {
  pagination: PageRequest | undefined;
}

/** QueryRestrictedAccountsResponse is the response type for Query/RestrictedAccounts. */
export interface QueryRestrictedAccountsResponse {
  accounts: RestrictedAccount[];
  pagination: PageResponse | undefined;
}

/** QueryValidatorPerformanceRequest is the request type for Query/ValidatorPerformance. */
export interface QueryValidatorPerformanceRequest {
  validatorAddress: string;
}

/** QueryValidatorPerformanceResponse is the response type for Query/ValidatorPerformance. */
export interface QueryValidatorPerformanceResponse {
  performance: ValidatorPerformance | undefined;
}

function createBaseQueryParamsRequest(): QueryParamsRequest {
  return {};
}

export const QueryParamsRequest: MessageFns<QueryParamsRequest> = {
  encode(_: QueryParamsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryParamsRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): QueryParamsRequest {
    return {};
  },

  toJSON(_: QueryParamsRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(base?: I): QueryParamsRequest {
    return QueryParamsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsRequest>, I>>(_: I): QueryParamsRequest {
    const message = createBaseQueryParamsRequest();
    return message;
  },
};

function createBaseQueryParamsResponse(): QueryParamsResponse {
  return { params: undefined };
}

export const QueryParamsResponse: MessageFns<QueryParamsResponse> = {
  encode(message: QueryParamsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.params !== undefined) {
      Params.encode(message.params, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryParamsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryParamsResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.params = Params.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QueryParamsResponse {
    return { params: isSet(object.params) ? Params.fromJSON(object.params) : undefined };
  },

  toJSON(message: QueryParamsResponse): unknown {
    const obj: any = {};
    if (message.params !== undefined) {
      obj.params = Params.toJSON(message.params);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(base?: I): QueryParamsResponse {
    return QueryParamsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryParamsResponse>, I>>(object: I): QueryParamsResponse {
    const message = createBaseQueryParamsResponse();
    message.params = (object.params !== undefined && object.params !== null)
      ? Params.fromPartial(object.params)
      : undefined;
    return message;
  },
};

function createBaseQueryValidatorReserveRequest(): QueryValidatorReserveRequest {
  return {};
}

export const QueryValidatorReserveRequest: MessageFns<QueryValidatorReserveRequest> = {
  encode(_: QueryValidatorReserveRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryValidatorReserveRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryValidatorReserveRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(_: any): QueryValidatorReserveRequest {
    return {};
  },

  toJSON(_: QueryValidatorReserveRequest): unknown {
    const obj: any = {};
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryValidatorReserveRequest>, I>>(base?: I): QueryValidatorReserveRequest {
    return QueryValidatorReserveRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryValidatorReserveRequest>, I>>(_: I): QueryValidatorReserveRequest {
    const message = createBaseQueryValidatorReserveRequest();
    return message;
  },
};

function createBaseQueryValidatorReserveResponse(): QueryValidatorReserveResponse {
  return { reserve: undefined, nextRelease: "" };
}

export const QueryValidatorReserveResponse: MessageFns<QueryValidatorReserveResponse> = {
  encode(message: QueryValidatorReserveResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.reserve !== undefined) {
      ValidatorReserve.encode(message.reserve, writer.uint32(10).fork()).join();
    }
    if (message.nextRelease !== "") {
      writer.uint32(18).string(message.nextRelease);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryValidatorReserveResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryValidatorReserveResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.reserve = ValidatorReserve.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.nextRelease = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QueryValidatorReserveResponse {
    return {
      reserve: isSet(object.reserve) ? ValidatorReserve.fromJSON(object.reserve) : undefined,
      nextRelease: isSet(object.nextRelease)
        ? globalThis.String(object.nextRelease)
        : isSet(object.next_release)
        ? globalThis.String(object.next_release)
        : "",
    };
  },

  toJSON(message: QueryValidatorReserveResponse): unknown {
    const obj: any = {};
    if (message.reserve !== undefined) {
      obj.reserve = ValidatorReserve.toJSON(message.reserve);
    }
    if (message.nextRelease !== "") {
      obj.nextRelease = message.nextRelease;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryValidatorReserveResponse>, I>>(base?: I): QueryValidatorReserveResponse {
    return QueryValidatorReserveResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryValidatorReserveResponse>, I>>(
    object: I,
  ): QueryValidatorReserveResponse {
    const message = createBaseQueryValidatorReserveResponse();
    message.reserve = (object.reserve !== undefined && object.reserve !== null)
      ? ValidatorReserve.fromPartial(object.reserve)
      : undefined;
    message.nextRelease = object.nextRelease ?? "";
    return message;
  },
};

function createBaseQueryRestrictedAccountRequest(): QueryRestrictedAccountRequest {
  return { address: "" };
}

export const QueryRestrictedAccountRequest: MessageFns<QueryRestrictedAccountRequest> = {
  encode(message: QueryRestrictedAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.address !== "") {
      writer.uint32(10).string(message.address);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRestrictedAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryRestrictedAccountRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.address = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QueryRestrictedAccountRequest {
    return { address: isSet(object.address) ? globalThis.String(object.address) : "" };
  },

  toJSON(message: QueryRestrictedAccountRequest): unknown {
    const obj: any = {};
    if (message.address !== "") {
      obj.address = message.address;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryRestrictedAccountRequest>, I>>(base?: I): QueryRestrictedAccountRequest {
    return QueryRestrictedAccountRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryRestrictedAccountRequest>, I>>(
    object: I,
  ): QueryRestrictedAccountRequest {
    const message = createBaseQueryRestrictedAccountRequest();
    message.address = object.address ?? "";
    return message;
  },
};

function createBaseQueryRestrictedAccountResponse(): QueryRestrictedAccountResponse {
  return { account: undefined, available: "" };
}

export const QueryRestrictedAccountResponse: MessageFns<QueryRestrictedAccountResponse> = {
  encode(message: QueryRestrictedAccountResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.account !== undefined) {
      RestrictedAccount.encode(message.account, writer.uint32(10).fork()).join();
    }
    if (message.available !== "") {
      writer.uint32(18).string(message.available);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRestrictedAccountResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryRestrictedAccountResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.account = RestrictedAccount.decode(reader, reader.uint32());
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.available = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QueryRestrictedAccountResponse {
    return {
      account: isSet(object.account) ? RestrictedAccount.fromJSON(object.account) : undefined,
      available: isSet(object.available) ? globalThis.String(object.available) : "",
    };
  },

  toJSON(message: QueryRestrictedAccountResponse): unknown {
    const obj: any = {};
    if (message.account !== undefined) {
      obj.account = RestrictedAccount.toJSON(message.account);
    }
    if (message.available !== "") {
      obj.available = message.available;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryRestrictedAccountResponse>, I>>(base?: I): QueryRestrictedAccountResponse {
    return QueryRestrictedAccountResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryRestrictedAccountResponse>, I>>(
    object: I,
  ): QueryRestrictedAccountResponse {
    const message = createBaseQueryRestrictedAccountResponse();
    message.account = (object.account !== undefined && object.account !== null)
      ? RestrictedAccount.fromPartial(object.account)
      : undefined;
    message.available = object.available ?? "";
    return message;
  },
};

function createBaseQueryRestrictedAccountsRequest(): QueryRestrictedAccountsRequest {
  return { pagination: undefined };
}

export const QueryRestrictedAccountsRequest: MessageFns<QueryRestrictedAccountsRequest> = {
  encode(message: QueryRestrictedAccountsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.pagination !== undefined) {
      PageRequest.encode(message.pagination, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRestrictedAccountsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryRestrictedAccountsRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.pagination = PageRequest.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QueryRestrictedAccountsRequest {
    return { pagination: isSet(object.pagination) ? PageRequest.fromJSON(object.pagination) : undefined };
  },

  toJSON(message: QueryRestrictedAccountsRequest): unknown {
    const obj: any = {};
    if (message.pagination !== undefined) {
      obj.pagination = PageRequest.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryRestrictedAccountsRequest>, I>>(base?: I): QueryRestrictedAccountsRequest {
    return QueryRestrictedAccountsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryRestrictedAccountsRequest>, I>>(
    object: I,
  ): QueryRestrictedAccountsRequest {
    const message = createBaseQueryRestrictedAccountsRequest();
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageRequest.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryRestrictedAccountsResponse(): QueryRestrictedAccountsResponse {
  return { accounts: [], pagination: undefined };
}

export const QueryRestrictedAccountsResponse: MessageFns<QueryRestrictedAccountsResponse> = {
  encode(message: QueryRestrictedAccountsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.accounts) {
      RestrictedAccount.encode(v!, writer.uint32(10).fork()).join();
    }
    if (message.pagination !== undefined) {
      PageResponse.encode(message.pagination, writer.uint32(18).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryRestrictedAccountsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryRestrictedAccountsResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.accounts.push(RestrictedAccount.decode(reader, reader.uint32()));
            continue;
          }
          case 2: {
            if (tag !== 18) {
              break;
            }

            message.pagination = PageResponse.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QueryRestrictedAccountsResponse {
    return {
      accounts: globalThis.Array.isArray(object?.accounts)
        ? object.accounts.map((e: any) => RestrictedAccount.fromJSON(e))
        : [],
      pagination: isSet(object.pagination) ? PageResponse.fromJSON(object.pagination) : undefined,
    };
  },

  toJSON(message: QueryRestrictedAccountsResponse): unknown {
    const obj: any = {};
    if (message.accounts?.length) {
      obj.accounts = message.accounts.map((e) => RestrictedAccount.toJSON(e));
    }
    if (message.pagination !== undefined) {
      obj.pagination = PageResponse.toJSON(message.pagination);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryRestrictedAccountsResponse>, I>>(base?: I): QueryRestrictedAccountsResponse {
    return QueryRestrictedAccountsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryRestrictedAccountsResponse>, I>>(
    object: I,
  ): QueryRestrictedAccountsResponse {
    const message = createBaseQueryRestrictedAccountsResponse();
    message.accounts = object.accounts?.map((e) => RestrictedAccount.fromPartial(e)) || [];
    message.pagination = (object.pagination !== undefined && object.pagination !== null)
      ? PageResponse.fromPartial(object.pagination)
      : undefined;
    return message;
  },
};

function createBaseQueryValidatorPerformanceRequest(): QueryValidatorPerformanceRequest {
  return { validatorAddress: "" };
}

export const QueryValidatorPerformanceRequest: MessageFns<QueryValidatorPerformanceRequest> = {
  encode(message: QueryValidatorPerformanceRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.validatorAddress !== "") {
      writer.uint32(10).string(message.validatorAddress);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryValidatorPerformanceRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryValidatorPerformanceRequest();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.validatorAddress = reader.string();
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QueryValidatorPerformanceRequest {
    return {
      validatorAddress: isSet(object.validatorAddress)
        ? globalThis.String(object.validatorAddress)
        : isSet(object.validator_address)
        ? globalThis.String(object.validator_address)
        : "",
    };
  },

  toJSON(message: QueryValidatorPerformanceRequest): unknown {
    const obj: any = {};
    if (message.validatorAddress !== "") {
      obj.validatorAddress = message.validatorAddress;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryValidatorPerformanceRequest>, I>>(
    base?: I,
  ): QueryValidatorPerformanceRequest {
    return QueryValidatorPerformanceRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryValidatorPerformanceRequest>, I>>(
    object: I,
  ): QueryValidatorPerformanceRequest {
    const message = createBaseQueryValidatorPerformanceRequest();
    message.validatorAddress = object.validatorAddress ?? "";
    return message;
  },
};

function createBaseQueryValidatorPerformanceResponse(): QueryValidatorPerformanceResponse {
  return { performance: undefined };
}

export const QueryValidatorPerformanceResponse: MessageFns<QueryValidatorPerformanceResponse> = {
  encode(message: QueryValidatorPerformanceResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.performance !== undefined) {
      ValidatorPerformance.encode(message.performance, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): QueryValidatorPerformanceResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const previousRecursionDepth = (reader as any).__tsProtoDecodeDepth ?? 0;
    if (previousRecursionDepth >= 100) {
      throw new globalThis.Error("protobuf decode recursion limit exceeded");
    }
    (reader as any).__tsProtoDecodeDepth = previousRecursionDepth + 1;
    try {
      const end = length === undefined ? reader.len : reader.pos + length;
      const message = createBaseQueryValidatorPerformanceResponse();
      while (reader.pos < end) {
        const tag = reader.uint32();
        switch (tag >>> 3) {
          case 1: {
            if (tag !== 10) {
              break;
            }

            message.performance = ValidatorPerformance.decode(reader, reader.uint32());
            continue;
          }
        }
        if ((tag & 7) === 4 || tag === 0) {
          break;
        }
        reader.skip(tag & 7);
      }
      return message;
    } finally {
      (reader as any).__tsProtoDecodeDepth = previousRecursionDepth;
    }
  },

  fromJSON(object: any): QueryValidatorPerformanceResponse {
    return { performance: isSet(object.performance) ? ValidatorPerformance.fromJSON(object.performance) : undefined };
  },

  toJSON(message: QueryValidatorPerformanceResponse): unknown {
    const obj: any = {};
    if (message.performance !== undefined) {
      obj.performance = ValidatorPerformance.toJSON(message.performance);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<QueryValidatorPerformanceResponse>, I>>(
    base?: I,
  ): QueryValidatorPerformanceResponse {
    return QueryValidatorPerformanceResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<QueryValidatorPerformanceResponse>, I>>(
    object: I,
  ): QueryValidatorPerformanceResponse {
    const message = createBaseQueryValidatorPerformanceResponse();
    message.performance = (object.performance !== undefined && object.performance !== null)
      ? ValidatorPerformance.fromPartial(object.performance)
      : undefined;
    return message;
  },
};

/** Query defines the gRPC querier service. */
export interface Query {
  /** Parameters queries the parameters of the module. */
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse>;
  /** ValidatorReserve queries the remaining validator reserve and epoch state. */
  ValidatorReserve(request: QueryValidatorReserveRequest): Promise<QueryValidatorReserveResponse>;
  /** RestrictedAccount queries the creator/team transfer limit state of an account. */
  RestrictedAccount(request: QueryRestrictedAccountRequest): Promise<QueryRestrictedAccountResponse>;
  /** RestrictedAccounts lists all restricted accounts. */
  RestrictedAccounts(request: QueryRestrictedAccountsRequest): Promise<QueryRestrictedAccountsResponse>;
  /** ValidatorPerformance queries the uptime/quality counters of a validator. */
  ValidatorPerformance(request: QueryValidatorPerformanceRequest): Promise<QueryValidatorPerformanceResponse>;
}

export const QueryServiceName = "ferac.ferac.v1.Query";
export class QueryClientImpl implements Query {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || QueryServiceName;
    this.rpc = rpc;
    this.Params = this.Params.bind(this);
    this.ValidatorReserve = this.ValidatorReserve.bind(this);
    this.RestrictedAccount = this.RestrictedAccount.bind(this);
    this.RestrictedAccounts = this.RestrictedAccounts.bind(this);
    this.ValidatorPerformance = this.ValidatorPerformance.bind(this);
  }
  Params(request: QueryParamsRequest): Promise<QueryParamsResponse> {
    const data = QueryParamsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "Params", data);
    return promise.then((data) => QueryParamsResponse.decode(new BinaryReader(data)));
  }

  ValidatorReserve(request: QueryValidatorReserveRequest): Promise<QueryValidatorReserveResponse> {
    const data = QueryValidatorReserveRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ValidatorReserve", data);
    return promise.then((data) => QueryValidatorReserveResponse.decode(new BinaryReader(data)));
  }

  RestrictedAccount(request: QueryRestrictedAccountRequest): Promise<QueryRestrictedAccountResponse> {
    const data = QueryRestrictedAccountRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RestrictedAccount", data);
    return promise.then((data) => QueryRestrictedAccountResponse.decode(new BinaryReader(data)));
  }

  RestrictedAccounts(request: QueryRestrictedAccountsRequest): Promise<QueryRestrictedAccountsResponse> {
    const data = QueryRestrictedAccountsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "RestrictedAccounts", data);
    return promise.then((data) => QueryRestrictedAccountsResponse.decode(new BinaryReader(data)));
  }

  ValidatorPerformance(request: QueryValidatorPerformanceRequest): Promise<QueryValidatorPerformanceResponse> {
    const data = QueryValidatorPerformanceRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "ValidatorPerformance", data);
    return promise.then((data) => QueryValidatorPerformanceResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
